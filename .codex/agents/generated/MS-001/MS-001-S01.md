---
{
  "schema_version": "stage-prompt/v1",
  "prompt_pack_execution": {
    "plan_doc": "../../../docs/architecture/planning/milestones/MS-001/plan.md",
    "prompt_pack_dir": "../../agents/generated/MS-001",
    "stage_ledger": "../../../delivery/ledgers/MS-001.md"
  },
  "stage_contract": {
    "id": "MS-001-S01",
    "title": "Define the delivery contract and comparison baseline",
    "prompt_path": "../../agents/generated/MS-001/MS-001-S01.md",
    "report_path": "../evidence/MS-001/MS-001-S01/report.md",
    "receipt_dir": "../evidence/MS-001/MS-001-S01/receipts",
    "depends_on": [],
    "expected_touches": [
      "../../../deploy/compose/delivery-manifest.schema.json",
      "../../../deploy/compose/delivery-verification-policy.json",
      "../../../docs/architecture/runtime-network-installation.md",
      "../../../tests/tooling/test_delivery_bundle.py"
    ],
    "acceptance_criteria": [
      "MS-001/AC-01: satisfy the stage-specific contribution and evidence defined in accepted plan 1.1.0 and this prompt.",
      "MS-001/AC-04: satisfy the stage-specific contribution and evidence defined in accepted plan 1.1.0 and this prompt.",
      "MS-001/AC-05: satisfy the stage-specific contribution and evidence defined in accepted plan 1.1.0 and this prompt.",
      "MS-001/AC-06: satisfy the stage-specific contribution and evidence defined in accepted plan 1.1.0 and this prompt.",
      "MS-001/AC-09: satisfy the stage-specific contribution and evidence defined in accepted plan 1.1.0 and this prompt."
    ],
    "proof_boundary": "Validated source-level delivery contract, complete inventory and frozen measurement protocol; no image, external publication, runtime or performance result is claimed.",
    "validation": {
      "profile": "prompt-pack/v1",
      "checks": [
        "Validate real schema/policy files and positive/negative fixtures; focused tests added for their actual parser boundary",
        "Review old valid/invalid release env compatibility and complete service/file/migration inventory",
        "Review the source-bound measurement protocol, available native targets and exact proposed provider/trust inputs",
        "Run applicable Markdown/contract/tooling checks from docs/architecture/tooling-gates.md"
      ],
      "requires_user_acceptance": false
    },
    "entry_inputs": [
      {
        "path": "../../../docs/architecture/planning/milestones/MS-001/plan.md",
        "producer_stage": null
      }
    ]
  },
  "plan_binding": {
    "version": "1.1.0",
    "sha256": "e42e4106de6a30698ab5732504090f6379f3dbda74020d00bb302447c4fad8a1"
  },
  "context": {
    "always_read": [
      "AGENTS.md",
      ".codex/AGENTS.md",
      "docs/architecture/planning/milestones/MS-001/plan.md",
      ".codex/delivery/ledgers/MS-001.md"
    ],
    "task_entrypoints": [
      "deploy/compose/validate-release-manifest.py",
      "compose.yaml",
      "docs/architecture/runtime-network-installation.md",
      "tools/custometry_quality/gate_performance.py"
    ]
  }
}
---

# MS-001-S01. Define the delivery contract and comparison baseline

Plan: [MS-001 1.1.0](../../../../docs/architecture/planning/milestones/MS-001/plan.md). Journal: [MS-001](../../../delivery/ledgers/MS-001.md).

## Entry and scope

Use `staged-plan-runner` for this one stage. Read the current journal before acting; mutable permission, claims, decisions and current state are deliberately absent from this prompt. Check the bound plan digest, actual inputs, accepted predecessor receipts and current execution authority. Use the repository `custometry-stage-ledger/v1` updater and bound evidence from the journal. Never change live journal JSON by hand or copy another session identity. The authoring conversation is not required.

Read root `AGENTS.md`, `.codex/AGENTS.md`, the exact plan and the current journal first. Then use only the entrypoints below. Read selected machine/human clauses for their actual requirement IDs; runtime/tooling contracts and directly affected readers resolve boundaries. Expand only for a named missing dependency, source conflict or failing check; no whole-repository audit is implied.

Preserve foreign edits. Resolve the declared touch zones to concrete paths before writes and change only safely separable in-scope content. No named executor, task owner, role assignment or organization approval is required by this pack. Technical decisions inside the accepted scope are delegated; a material scope/security exception needs its concrete evidence and the user's decision. No Git commit/push/PR, dependency or credential provisioning, external publication, deployment or destructive host cleanup is granted by a prompt alone. Complete authorized independent/local preparation while a specific external effect is unavailable.

The accepted supply boundary is core plus explicit optional demo, immutable retrieval and repeat-build comparison, ARM64+AMD64 image proof, and bundle authentication with public images permitted. Full installation remains M5+ARM64 VM in C03. Reuse current delivery seams; preserve strict manifests, migration separation, state safety and candidate/full-release distinctions. Do not reopen these choices.

## Task entrypoints

- `deploy/compose/validate-release-manifest.py`
- `compose.yaml`
- `docs/architecture/runtime-network-installation.md`
- `tools/custometry_quality/gate_performance.py`

## Work and observable outputs

Use these repository commands after reading the declared context:

```sh
source scripts/activate-toolchain.sh
uv run --locked python -m tools.custometry_quality.stage_ledger preflight --ledger .codex/delivery/ledgers/MS-001.md --stage MS-001-S01
uv run --locked python -m tools.custometry_quality.stage_ledger claim --ledger .codex/delivery/ledgers/MS-001.md --stage MS-001-S01 --expected-sha256 <sha-returned-by-preflight>
```

`claim` requires the current execution request. Use the actual `CODEX_THREAD_ID`
session (automatic), or an actual stable runner session via `--session`; never
invent an assignee. Remain in the canonical local checkout. The updater returns
the new journal SHA for subsequent transactions. A stale hash requires rereading;
a foreign/stale claim requires explicit reconciliation. No stage side effects
precede the successful claim. If the predecessor is already accepted and this
pending stage remains disallowed, review actual current inputs and authority,
record readiness evidence, and use `stage_ledger advance` with this stage ID,
`--reason`, `--evidence` and the observed journal hash before preflight. This
preserves the accepted predecessor and its receipt; it does not bypass missing
inputs or user decisions. Full CLI ownership/recovery semantics are in
[tooling gates section 7](../../../../docs/architecture/tooling-gates.md#7-stage-journal-transactions).

1. Resolve the actual core service-to-image mapping and the accepted optional demo profile; inventory embedded migrations, tracked assets, relative mounts, required host utilities and all declared dependencies. Inspect direct readers of the old three-key env contract. Preserve its strict validation and expected GHCR origin.
2. Create the bounded structured manifest schema at `deploy/compose/delivery-manifest.schema.json` and a non-secret verification policy at `deploy/compose/delivery-verification-policy.json`. The policy records schema/reader compatibility, origin/signer trust inputs and finite limits; it is product delivery input, not a second execution ledger. Use existing runtime documentation for semantics and metrics, not a new framework.
3. Specify canonical serialization/signature subject rules, OCI index versus platform-child digests, file/resource closure, migration head/hash/compatibility, and the derived env adapter. Explicitly define path, symlink, duplicate/archive entry, size and decompression checks; do not execute untrusted bundle content to validate it.
4. Read the actual existing GitHub/GHCR access/retention behavior and select the smallest authenticated bundle mechanism. The accepted policy is authentication for the bundle only; public Custometry images are permitted. Record exact proposed target, credential delivery, unavailable/expired-artifact behavior and retention/handoff-copy policy. Read-only preparation does not authorize publication or account-policy changes.
5. Resolve real signer workflow/ref/issuer, pinned verifier/scanner/action versions and initial independent verifier trust. Consult current primary documentation for volatile tool semantics. Identify tool credentials/permissions needed later without requesting or printing secrets. Record available native ARM64/AMD64 proof targets; do not invent a machine or claim an unavailable runner.
6. Freeze the plan section 8 protocol: exact timer start/end boundaries, baseline source/commit, commands, fixture hashes, cold/warm modes, run counts, image/resource caps and screenshot states. Where no old bundle exists, designate first successful implementation as the baseline. Keep timing acceptance unassessed without a selected numeric threshold; do not manufacture gate_performance input.
7. Add focused schema/fixture tests for valid input and the material negative boundaries. Update existing runtime/tooling documentation only where the new contract makes it stale. Record a concrete decision/proof packet for S02/S03/S04 in this stage report; no task-owner table or extra approval checkpoint.

## Required checks

- Validate real schema/policy files and positive/negative fixtures; focused tests added for their actual parser boundary.
- Review old valid/invalid release env compatibility and complete service/file/migration inventory.
- Review the source-bound measurement protocol, available native targets and exact proposed provider/trust inputs.
- Run applicable Markdown/contract/tooling checks from docs/architecture/tooling-gates.md.

## Conditional skills and verification discipline

- Use `contract-impact-analysis` when schema/config/readers or another relied-upon boundary changes; retain separate classifications and supported transitions.
- Use `backend-quality-gates` for changed Python/tests and `root-cause-debugging` only for a concrete unexplained failure. Run the smallest meaningful checks from the repository tooling contract.
- Use `backend-performance-evidence` when designing/collecting a comparison or making any speed/resource claim; record baseline, conditions, metric and uncertainty. Do not optimize unrelated code or invent an acceptance threshold.
- Use `browser-qa-evidence` when real browser proof is required; use `playwright-cli` for selected terminal mechanics unless the user selected Browser/Chrome. Add a relevant `better-*` skill only for a materially changed interface domain. C02 artifact proof does not authorize product UI redesign or later auth-flow implementation.
- Use `architecture-design` only if a material unresolved target decision actually appears, and `prompt-manager` only for an authorized pack amendment. No retired G-program, Penpot or live Figma route is active.

Use `source scripts/activate-toolchain.sh` before grouped repository profiles. Exact module/test invocations come from current tooling and the stage's real implementation; future tool outputs are dependencies, not commands assumed to exist. Gate results must be actual pass/fail/unavailable observations. Keep secrets, tokens, cookies, raw provider payloads, environment dumps and sensitive build arguments out of durable evidence. No full release/performance verdict follows from passing a subset.

## Handoff and documentation

Write the redacted stage report to the contract path with actual created/modified/deleted paths, excluded foreign edits, command results, criterion coverage, residual limits, measured comparisons where applicable, and exact outputs consumed next. Preserve immutable iteration evidence and old receipts. Update affected canonical docs/indexes in the same unit; do not create a new control document or status board.

Use the installed `prompt-manager/references/transition-receipt-v1.md` format: a new immutable receipt binds canonical stage-contract SHA-256, live plan/prompt/report and real validation evidence. Final user acceptance is recorded only when the stage contract requires it; unresolved final acceptance yields review_ready and no next-stage permission. The runner uses `python -m tools.custometry_quality.stage_ledger accept --ledger .codex/delivery/ledgers/MS-001.md --stage MS-001-S01 --expected-sha256 <current-observed-sha> --receipt <journal-relative-new-receipt>` after semantic review of the actual checks. The CLI validates live bindings, records the outcome and verifies any next-stage allowance atomically. Use `pause`/`resume` for a concrete missing decision and `block` for a hard blocker, with the evidence and current hash required by tooling gates. Preserve previous reports/receipts before revising a reviewed result. Never reinitialize the journal.

Inspect the exact next prompt and its producer inputs before proposing advancement. Current-stage completion and next-stage permission are separate. Stop after this stage in manual sequential mode; never start another milestone or Goal automatically. The final stage has no successor.
