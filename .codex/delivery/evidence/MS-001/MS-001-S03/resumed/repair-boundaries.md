# S03 resumed repair boundaries

Owner resolution authorizes necessary repairs and exact supply effects; the original default license policy and critical-zero requirement remain unchanged. This is in-progress evidence, not stage acceptance.

- Naming/reader/reconciliation: compatible correction to the accepted S01 version/run/attempt contract; wrong names and replacement of content across attempts fail.
- License parsing: compatible support for SPDX conjunctions/choices and installed source/hash-bound metadata; review-required/unlisted/denied obligations stay fail unless separately proved under the permitted component-specific review mechanism. No review approval is fabricated.
- API packaging: compatible intended Python 3.12 runtime closure, updated to 3.12.14/trixie; all venv/application/std-lib files and linked ELF libraries retained, base pip/admin binaries excluded. Exact copied distro package metadata/copyright remain. Diagnostic ARM64 API6 build/imports pass with psycopg3.2.9 C, libpq17.11 and systemOpenSSL3.5.7; its raw Trivy critical/unknown count is zero. Final clean repeated builds, migration and both-native proof remain pending. No application API, enabled capability, migration bytes or stored data changed.
- Web: same Nginx major 1, security update 1.30.4 Alpine 3.24 slim; actual base ARM64 critical/unknown scan is zero. Final product/config/native proof is pending.
- PostgreSQL: upstream17.11 Alpine3.23 replaces17.5, synchronized in Compose/schema/producer/fixtures. Same major17 and fresh-owned database boundary; installed upgrade/downgrade remains unqualified. Two scanner records on gosu require exact platform/binary applicability evidence; no image rebuild is attributed to Custometry.

Raw base scans, failed earlier candidate outcomes, fresh DB identity and original license findings remain adjacent. New supply jobs are still disabled during local preparation. Scoped Git commits may capture exact build input; no complete signed bundle is exposed before all mandatory gates pass.

The container-only driver extra preserves local development's existing binary wheel,
while hash-pinned runtime Debian packages eliminate the wheel's old private OpenSSL.
Build5 exposed a pre-1980 wheel ZIP timestamp failure; SOURCE_DATE_EPOCH now uses
1980-01-01 (315532800) and build6 passes. Tooling tests277 and subsequent focused42
passed; final source-profile evidence remains pending.

Web chunk graph/notice binding is a compatible supply-evidence change: actual Rollup
included modules, package metadata and emitted chunk hashes replace the conservative
installed-build-package notice selection. Its local173-module/20-package inventory
has all notice texts, and a diagnostic SBOM scan has zero critical/unknown findings.
The latter uses a deliberate diagnostic placeholder subject and is never final OCI
proof. Generated docs/assets and actual final native Web image remain unproved.

The coordinator confirmed authority for a corresponding-source companion route if
actual minimum source sizes require it. The current single-ZIP v1 closure promise
would change: a new trusted reader/policy/schema, mandatory whole-set verification,
finite count/byte bounds, immutable identities and retention would be required.
No companion contract or artifact is implemented at this checkpoint; source sizes
and obligations are still being established. Existing terminal S01/S02 records stay
unchanged and any later revision must be explicit.
