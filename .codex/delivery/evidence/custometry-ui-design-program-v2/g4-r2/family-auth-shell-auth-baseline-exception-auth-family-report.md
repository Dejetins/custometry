---
artifact_kind: ui_design_program_stage_execution_report
program_id: CUSTOMETRY-UI-DESIGN-PROGRAM-V2
stage_instance_id: G4@family.auth.shell-auth.baseline-exception-auth-r2
status: blocked
proof_boundary: canonical G4 reference-capture contract compatibility; no finished family visual, family acceptance, owner review, production implementation, publication, deployment, mobile-specific scope, or adjacent-stage execution
---

# G4 family.auth.shell-auth.baseline-exception-auth execution report

## Outcome

The exact G4 row was preflighted, uniquely claimed, and stopped on a hard
canonical proof-contract incompatibility before family artifacts or browser
acceptance receipts were created. The accepted pilot cannot be used as the
required `visual_language_conformance` reference with the same exhaustive
actionful target-screen contract without changing accepted bytes, injecting
unpinned runtime behavior, or fabricating evidence.

## Verified frontier

- The authoritative triad, exact prompt, program, G2 family slice,
  `UI-AUTH-001` slice, fixed baseline, accepted pilot, G3 review board, G3
  owner decision, and incoming `ready` transition matched their dispatched
  SHA-256 bindings before claim.
- The ledger-bound context resolved 8/16 files at approximately 43,554 tokens
  before claim and 8/16 files at approximately 43,569 tokens after claim.
- Exactly 26 G4 rows were pending and unclaimed before the selected row won
  claim `codex-root-g4-01-019ff348-20260812T000614Z`.
- `ui_design_tool.py preflight` passed with Chromium `150.0.7871.187`.

## Hard blocker

The current screen-contract validator requires
`interaction_contract.visible_actions` to exact-cover every functional action,
including `UI-AUTH-001.inspect`. The canonical capture tool executes those
actions and validates fixture/font/asset meta bindings for both the reference
and implementation targets. The hash-pinned accepted pilot contains none of
those target-screen bindings. At the same time, the comparison and visual-QA
chain requires reference render provenance to name that exact pilot path and
SHA-256.

Therefore no honest receipt can pass the current chain. A wrapper, edited
pilot, injected subresource, candidate-derived reference, or hand-edited
receipt is explicitly forbidden by the visual-provenance contract and this
dispatch.

## Evidence

- Runtime blocker: `.codex/delivery/ui-design-programs/custometry-v2/evidence/g4-r2/family-auth-shell-auth-baseline-exception-auth/runtime-proof-blocker.json`
- Stage ledger: `.codex/delivery/ui-design-programs/custometry-v2/stage-ledger.md`
- Accepted incoming transition: `.codex/delivery/ui-design-programs/custometry-v2/evidence/g3-r2/stage-transition.json`

## Write scope and foreign changes

This execution created only the blocker evidence and this report, and modified
only the selected ledger row/frontier. All accepted G0-G3 artifacts, prompts,
pilot files, production code, other G4 rows, G5/G6 rows, and foreign working
tree changes were preserved.

## Required repair

A revisioned release of the canonical `ui-design-program` capture/visual-QA
contract must separate accepted visual-language reference capture from
target-screen functional-action and fixture-meta execution, while keeping the
reference hash-pinned and retaining implementation-side interaction proof.
That installed-skill maintenance is outside the exact G4 program-owned write
scope and requires separate authority.
