---
task_schema: codex.executor-task/v1
task_id: custometry-v2-ui-standard-v2-compatibility-to-g4-review-ready-r1
artifact_kind: non_authoritative_executor_dispatch
language: en
classification: ui_design_skill_compatibility_repair_program_migration_and_auth_family_reproof
program_id: CUSTOMETRY-UI-DESIGN-PROGRAM-V2
execution_mode: goal_driven
goal_artifact_required: false
publication_authorized: false
deployment_authorized: false
mobile_scope: unauthorized
state_authority: stage_ledger_only
live_working_tree: /Users/daniildegtyarev/.codex/worktrees/e0a0/Custometry
source_thread_id: 019ff5bc-9204-7660-a3f4-76f28ec4e553
requested_terminal_target: migrated_g4_auth_family_review_ready_awaiting_owner_acceptance
authorized_sequence:
  - shared_skill_historical_control_plane_migration_repair
  - compatibility_preflight_and_change_impact
  - revisioned_pilot_metadata_candidate_and_owner_checkpoint_if_required
  - G0_successor
  - G1_successor
  - G2_successor
  - G3_successor_review_ready_owner_checkpoint
  - G4_auth_family_successor_review_ready
mandatory_owner_checkpoints:
  - exact_G0_successor_baseline_and_visual_authority
  - finished_G3_foundations_and_shell
  - finished_G4_auth_family_terminal_review
foreign_changes_excluded: true
---

# Objective

Migrate the live `CUSTOMETRY-UI-DESIGN-PROGRAM-V2` program from its historical
UI-design contract profiles to the current active `ui-design-program` contract,
close every compatibility gap between the current program/triad/evidence and
the new pilot-standard requirements, and return the current auth-family frontier
to a truthful, finished, browser-verifiable G4 `review_ready` result.

This is a revisioned compatibility migration, not permission to reinterpret the
product, change the accepted visual direction, implement production frontend
code, or silently accept a visual checkpoint. The final G4 result must remain
awaiting the owner's natural-language decision.

The required terminal state is:

```yaml
skill_compatibility_repair: complete
compatibility_migration: complete
active_contract_profiles: current_skill_release
g0_successor: accepted
g1_successor: accepted
g2_successor: accepted
g3_successor: accepted_by_owner
current_stage: G4_auth_family_successor
g4_family_gate: passing_family_review_ready
g4_result: finished_and_visually_reviewable
g4_transition_status: review_ready
g4_ledger_status: needs_input
ledger_status: awaiting_input
next_stage_allowed: false
executor_claim: preserved
owner_acceptance: pending
historical_rows_and_receipts: preserved_read_only
other_g4_rows: pending_unclaimed_unexecuted
g5_g6: pending_unclaimed_unexecuted
production_implementation: untouched
publication_deployment_mobile: untouched
```

Do not claim this terminal state if the G3 owner checkpoint has not been
unambiguously accepted or if the successor G4 family gate is not actually
passing under the active contract.

# Delivery classification

```yaml
classification: accepted_ui_program_requires_released_migration_repair_and_revisioned_contract_migration
execution_unit: goal_driven_coordinator_of_independently_bounded_skill_and_stage_units
primary_execution_skill: staged-plan-runner
contract_skill: ui-design-program
control_plane_skill: prompt-manager
browser_proof_skills:
  - browser-qa-evidence
  - playwright-cli
proof_boundary:
  - active UI-program contract compatibility
  - immutable visual-authority lineage
  - exact pilot-standard source inventory and clause coverage
  - corrected auth route/state identity
  - G3 foundations/shell browser proof and owner acceptance
  - G4 auth-family browser proof and review readiness
excluded_proof:
  - production frontend implementation
  - deployment or publication
  - mobile-specific design
  - full WCAG conformance
  - production authorization or real-data behavior
```

The user's request to close every compatibility gap and return the program to a
G4 review-ready checkpoint explicitly authorizes this coordinator to continue
through the complete declared sequence. `goal_driven` does not turn that
sequence into one unbounded stage: the shared-skill repair and every successor
G0/G1/G2/G3/G4 row remain separate execution units with their own bounded
prompt, context manifest, atomic claim, evidence, transition, ledger update and
report. Never hold two stage claims, batch claims, or cross an owner checkpoint
without the required typed response.

The dispatch is not a plan, ledger, acceptance receipt, or fourth execution
state authority. The stage ledger remains the only mutable execution-state
authority. Revalidate the live sources and follow higher current authority if
any dispatch snapshot is stale. Stop on a material conflict; do not edit a
source merely to make this dispatch appear correct.

# Exact live root and triad

Operate only in:

```text
/Users/daniildegtyarev/.codex/worktrees/e0a0/Custometry
```

Do not substitute the ambient checkout, `/Users/daniildegtyarev/Projects/Custometry`,
another worktree, or a reconstructed branch.

The live triad to migrate is:

```yaml
plan_doc: .codex/delivery/ui-design-programs/custometry-v2/ui-design-program.json
prompt_pack_dir: .codex/agents/generated/custometry-ui-design-g0-v2
stage_ledger: .codex/delivery/ui-design-programs/custometry-v2/stage-ledger.md
```

Current dispatch-time bindings, which must be revalidated before any write:

```yaml
plan_doc_sha256: 21810c42b847bfc591db9a7ad76cf3df64c6bb5ad0cb82dd80082bd0c7a124d2
stage_ledger_sha256: f7aaffd8fe1f57bf66dec3011b2de174880ab18d1a3fb74dea853df99cc1fed8
local_contract_registry_sha256: 1b8bf240f28c936831a78696dd763f0f08e77c730b8e1a56744530f83d9aa41b
accepted_pilot_source_sha256: d5639f0e20a79581972853979cd643ca456b6235c1ce098f5adc98d89636ca15
historical_platform_baseline_sha256: c0e6c547f61211c2ae7b21dde614a3dee1637667c156d03233f3138175f935a3
historical_intake_sha256: ead497ba8918b21b16ae2dac8ff5163eb968518938d016f40fc80900811f0b57
accepted_g3_owner_decision_sha256: 9bae65aed9e786a007f6fad1510605b0debf26fefdfc33c667e28e3191fbb6ea
current_g4_review_board_sha256: c80b5d4442dac1ed572c21675577816a08e2828cc0019cb9e1c37b543233329a
```

The stage-ledger hash is expected to change only through authorized lifecycle
and migration mutations. Other input bindings remain immutable until a typed
change record and the applicable owner decision establish a successor.

# Current live frontier at dispatch creation

```yaml
ledger_status: awaiting_input
current_stage: G4@family.auth.shell-auth.baseline-exception-auth-r3
next_stage_allowed: false
current_g4_status: needs_input
current_g4_current_authority: true
current_g4_execution_allowed: true
current_g4_executor_claim: codex-root-g4-01-r3-20260812T113149Z
historical_g4_r2_status: blocked
historical_g4_r2_current_authority: false
g0_r2_status: accepted
g1_r2_status: accepted
g2_r2_status: accepted
g3_r2_status: accepted
other_g4_rows: pending_unclaimed
g5_rows: pending_unclaimed
g6_row: pending_unclaimed
program_contract_profile: codex.ui-design-program/v1@1.6.0
baseline_contract_profile: codex.platform-ui-baseline/v1@1.0.0
screen_contract_profile: codex.ui-screen-design-contract/v1@1.4.0
runtime_profile: codex.ui-stage-runtime-profiles/v1@1.6.0
```

The active skill correctly rejects this frontier for active execution. At
dispatch creation:

- `program_ready` rejected the historical `1.6.0` program profile;
- `baseline_ready` rejected the sparse baseline and missing `standard_contract`;
- `validate_stage_ledger.py` rejected historical transition versions and every
  pending G4/G5/G6 prompt pinned to runtime `1.6.0`;
- `ui_program_context.py --ledger ...` returned `blocked` because the ledger is
  not semantically valid under the active contract.

These failures are migration inputs. Do not suppress them, weaken validators,
or hand-edit receipts.

# Verified current skill release

Read the current skill again at execution time. The dispatch-time release was:

```yaml
skill_path: /Users/daniildegtyarev/.codex/skills/ui-design-program/SKILL.md
skill_sha256: 08504b0b78792f7eed3067a9828b7ebc744f378dcbf50e41ccd458e3f04a5fcf
contract_versions_sha256: d091ef3037a05f7b44dd8b96dc940295bfc1cc3b64302da8d1fcf6943cb460ff
capture_ui_standard_sha256: 063df2cc1449f2215c81ca6ee4c5b2195bc310eb9468d23204c3c99dc2b67aa6
validate_skill_release_sha256: 775b64fc625218d17f7b4567b874e12b89d4fceb4ea6d1cc12275d13ddecc25c
runtime_profile: codex.ui-stage-runtime-profiles/v1@2.0.0
program_contract: codex.ui-design-program/v1@2.0.0
screen_contract: codex.ui-screen-design-contract/v1@2.0.0
baseline_contract: codex.platform-ui-baseline/v1@2.0.0
standard_source_capture: 2.0.0
standard_applicability_assembler: 1.0.0
capture_tool: 2.0.0
visual_qa_assembler: 2.0.0
screen_acceptance_assembler: 2.0.0
family_acceptance_assembler: 2.0.0
wave_acceptance_assembler: 2.0.0
implementation_handoff_assembler: 2.0.0
transition_assembler: 2.0.0
```

The former generator/schema mismatch is repaired. The following commands
passed at dispatch creation:

```text
python3 /Users/daniildegtyarev/.codex/skills/ui-design-program/scripts/validate_skill_release.py
python3 /Users/daniildegtyarev/.codex/skills/ui-design-program/scripts/validate_ui_standard_capture_chain.py --self-test
python3 /Users/daniildegtyarev/.codex/skills/ui-design-program/scripts/validate_ui_standard_regression.py --self-test
```

The real capture-chain self-test produced
`codex.ui-standard-source-inventory/v1@2.0.0`, with zero schema or semantic
errors, and rejected the stale `1.0.0` profile through both validators.

Treat the installed skill release as immutable input except for the one narrow
shared-skill compatibility repair described below. No other shared-skill
change is authorized.

## Mandatory shared-skill compatibility repair

The current release has one newly observed historical-migration deadlock:

- the live ledger cannot pass active `validate_stage_ledger.py` because its
  accepted historical receipts and future prompts use registered historical
  profiles;
- `assemble_change_impact.py` calls full active `validate_ledger` before it can
  assemble the typed receipt that must authorize invalidation and successors;
- therefore neither the first typed impact receipt nor the first valid active
  successor ledger can currently be produced canonically.

Before any Custometry lifecycle mutation, implement and release the smallest
canonical two-phase historical-control-plane migration surface inside:

```text
/Users/daniildegtyarev/.codex/skills/ui-design-program/**
```

The repair must:

1. accept only a parseable registered historical live triad whose exact paths,
   historical contract profiles, artifact hashes, terminal outcomes, claims,
   receipts and current non-terminal frontier can be independently verified;
2. distinguish registered historical contract evidence from corrupt, missing,
   ambiguous or fabricated state;
3. create a deterministic migration candidate and typed migration receipt that
   binds the exact source ledger hash, triad links, historical rows, current
   frontier, target active registry and proposed successor topology;
4. preserve all historical receipts and artifacts byte-for-byte while allowing
   the migrated active ledger to classify them as historical-only rather than
   revalidating them as newly assembled `2.0.0` receipts;
5. make the candidate ledger pass active semantic validation before an atomic
   replacement, or make the change-impact assembler consume an equally strict
   validated historical-migration view;
6. expose exactly one canonical first operation so the typed change record and
   change-impact receipt are assembled before authority invalidation is applied;
7. reject stale hashes, altered historical outcomes, missing claims/evidence,
   multiple current frontiers, competing claims, unregistered versions,
   dependency drift, hand-edited receipts and circular self-authorization;
8. add schemas/templates/registry/runtime integration where needed, positive
   and negative tests, a real historical-ledger-to-active-successor test,
   golden/cross-skill coverage and release-gate coverage;
9. preserve the stage ledger as the sole mutable state authority and avoid a
   parallel status file;
10. pass the full `validate_skill_release.py` gate before use on Custometry.

Use `prompt-manager` and `staged-plan-runner` contracts as the ownership
boundary. Do not weaken ordinary active ledger validation or create a generic
`--force` or skip-validation option. Record the newly released skill hashes in
the Custometry migration evidence, then use only that released canonical path
for the first control-plane mutation.

# Confirmed legacy-pilot compatibility gap

The accepted pilot source is immutable historical authority:

```text
.codex/delivery/ui-design-programs/custometry-v2/evidence/pilot-candidate-v2/ru/source.html
```

A read-only canonical capture of those exact bytes at `1440x900` succeeded but
observed:

```yaml
contract_profile: codex.ui-standard-source-inventory/v1@2.0.0
visible_nodes: 905
observations: 30770
component_identity_count: 0
shell_region_identity_count: 0
semantic_marker_nodes: 0
foundation_scope_nodes: 905
button_nodes: 55
```

The pilot predates `data-ui-component`, `data-ui-region`, `data-ui-variant`,
`data-ui-size`, `data-ui-state`, and `data-ui-reusable`. Therefore the active
capture is internally correct but cannot safely derive precise reusable
component applicability from the historical bytes. Generic `element_type`
selectors would over-apply distinct pilot variants to unrelated target
elements and recreate a false or contradictory standard.

Reproduce this preflight in a fresh temporary directory. Never reference the
dispatch creator's `/tmp` output as durable evidence.

## Required compatibility resolution

Do not mutate the accepted `pilot-candidate-v2` bytes, its owner receipt, or its
historical evidence. Do not annotate its live DOM during a
`visual_authority_reference` capture. Do not hand-edit canonical capture output.

If the reproduced canonical capture still lacks independently matchable
component/region/state identities, create a deterministic, program-owned,
revisioned pilot metadata candidate with these properties:

1. It is derived from the exact accepted v2 source by a deterministic builder.
2. It adds only non-rendering semantic `data-ui-*` metadata needed by the
   canonical source capture.
3. It does not change visible text, marketing copy, DOM element order,
   executable behavior, CSS, assets, fonts, network behavior, focus behavior,
   responsive behavior, or product semantics.
4. It never overwrites or replaces the v2 file in place.
5. It has its own exact path, hash, derivation receipt, and typed change record.
6. It exact-covers every visible node as either a stable reusable identity or
   one explicit `data-ui-reusable="false"` candidate whose disposition is
   justified from the source structure.
7. Its stable identities distinguish component, variant, size, slot, state,
   shell region, icon, semantic element type, and anchor wherever applicable.
8. Its rendered output is proven equivalent to v2 at every accepted responsive
   anchor and material source state under identical browser, viewport, locale,
   theme, clock, motion, font, asset, network-isolation, and redaction
   conditions.
9. The equivalence proof includes screenshots, pixel/raster support, computed
   geometry/style comparison, visible text/action inventory, keyboard/focus,
   console/network, overflow, and deterministic repeatability. Metadata-only
   attributes must be the only DOM difference.
10. It is not current visual authority until the mandatory G0 successor-
    baseline decision accepts the exact candidate, baseline, clause inventory
    and rendered standard board. Present a compact comparison/standard board
    and ask one plain-language decision. Do not ask the owner for hashes or
    revision syntax.

If a visual or behavioral difference appears, stop. Do not classify the
candidate as metadata-only and do not continue G0.

The G0 owner checkpoint is mandatory even if the original pilot bytes prove
sufficient and no metadata candidate is required. Any replacement or revision
of the fixed platform baseline requires an exact typed owner acceptance under
the active baseline contract. After acceptance, the exact approved source and
baseline become the successor authority. Preserve v2 as immutable historical
evidence. Capture the accepted successor bytes directly; do not wrap, inject,
or mutate them during reference proof.

# Authoritative product semantics and required correction

The visual-authority pilot fixes applicable reusable visual clauses. It does
not own target product meaning, target copy, target fixture truth, permissions,
routes, or screen-specific composition.

The current intake contains a material semantic error:

```yaml
incorrect_current_contract:
  screen_id: UI-AUTH-001
  route: /auth/sign-in
  invented_state: UI-AUTH-001.recovery
authoritative_product_contract:
  sign_in:
    screen_id: UI-AUTH-001
    route: /auth/sign-in
    surface_kind: editor
  recovery:
    screen_id: UI-AUTH-004
    route: /auth/recovery
    surface_kind: wizard
    meaning: Password recovery/reset
```

Use `custometry-ui-blueprint-ru.md` and
`packages/contracts/routes/ui-route-contracts.json` as the current route/screen
sources. Reconcile the exact state sets from those sources. Do not merely rename
the old `UI-AUTH-001.recovery` state. Recovery/reset must be represented as the
separate `UI-AUTH-004` route flow, with its own states, actions, outcomes,
failure/recovery behavior, and source bindings.

At G2, keep one bounded auth family for this terminal target and make both
`UI-AUTH-001` and `UI-AUTH-004` representatives. The existing family budget of
four screens and 24 screen/state pairs is sufficient for two six-state
representatives, subject to live revalidation. The common family grammar is
the auth shell and auth baseline exception; the two representatives may and
must have distinct route-specific composition. Do not treat reuse as identical
composition. If source-backed semantics or the live workload budget cannot
validate this one-family exact cover, stop before G4 and report a hard topology
conflict; this dispatch does not authorize a second G4 family or second family
owner gate. Do not claim individual design acceptance for `UI-AUTH-002` or
`UI-AUTH-003`. If a higher current source requires either as a representative,
stop because the terminal contract changed.

# Owner corrections that remain binding requirements

The current G4 r3 result was never accepted. Preserve it as historical,
unaccepted evidence. Carry these four owner correction receipts forward as
requirements, not as acceptance authority:

```text
.codex/delivery/ui-design-programs/custometry-v2/evidence/g4-r3/family-auth-shell-auth-baseline-exception-auth/owner-requested-changes-r3-01.json
.codex/delivery/ui-design-programs/custometry-v2/evidence/g4-r3/family-auth-shell-auth-baseline-exception-auth/owner-requested-changes-r3-02.json
.codex/delivery/ui-design-programs/custometry-v2/evidence/g4-r3/family-auth-shell-auth-baseline-exception-auth/owner-requested-changes-r3-03.json
.codex/delivery/ui-design-programs/custometry-v2/evidence/g4-r3/family-auth-shell-auth-baseline-exception-auth/owner-requested-changes-r3-04.json
```

Exact dispatch-time bindings:

```yaml
owner_requested_changes_r3_01_sha256: f40a4fddb84fead5f182111cf2e44d51711976df876d02e7f01aeec348554ec7
owner_requested_changes_r3_02_sha256: 886be10c124b940f964034b52b3451882219ee71bf4605ccd402fea0f0ab92fa
owner_requested_changes_r3_03_sha256: ebd5ee0e19085dce3c8014fc96753701a78b701c1487b884b96b00920d3199ef
owner_requested_changes_r3_04_sha256: ea1e6e64cb018c4cfa6488f635d7103730b3d268d1fc2e8bf4ebe478930a779a
```

Revalidate all four before use. Stop and classify any mismatch against live
owner-response evidence; do not silently adopt changed intent at the same path.

The successor auth-family result must:

- load all owner review surfaces under `file://` without broken assets;
- provide genuine keyboard- and pointer-operable state selection;
- contain no invented or non-functional marketing copy;
- conform to pilot-derived palette, surfaces, typography, radii, borders,
  density, controls, focus, motion, and responsive clauses;
- use a compact sign-in surface derived from the pilot-standard measurements,
  not a target-authored arbitrary width;
- present recovery/reset as a structurally distinct `UI-AUTH-004` flow, not a
  relabeled sign-in form;
- include a machine-checkable state/route distinction that prevents this error
  from passing again;
- preserve Result Trust, safe error/retry behavior, RU/EN structural stress,
  200% zoom/reflow, reduced motion, keyboard/focus, accessibility smoke, and
  the authorized responsive-Web range;
- keep mobile-specific scope excluded.

The existing `baseline-exception-auth` permits only shell-region presence and
composition required by the source-defined auth surface class. It does not
permit independent typography, control-size, radius, palette, density, focus,
or motion drift. Any further exception must be one narrow, clause-bound, typed,
owner-accepted exception; otherwise it fails.

# Complete compatibility gap closure

Close all of the following. Do not stop after merely changing version strings.

## Contract registry and program-owned contract copies

- Replace the active local contract registry with a revision that matches the
  installed skill's active registry exactly for every active key.
- Retain registered historical profiles only as read-only inputs.
- Synchronize every program-owned schema/template/runtime asset that is used by
  an active successor row.
- Add the source-inventory, standard-applicability, and review-board contract
  assets required by the active release when absent.
- Do not overwrite historical receipts or claim that old generated artifacts
  were produced by new assemblers.
- Record exact source skill file hashes and deterministic copy provenance.

## G0 successor

- Create one typed `codex.ui-change-record/v1` anchored to the exact accepted
  current-authority artifact/ledger row selected by the active change-impact
  matrix.
- Assemble, never hand-edit, the change-impact receipt.
- Classify the contract/baseline migration at the earliest affected gate `G0`.
- Create a new `codex.ui-design-program/v1@2.0.0` program revision, active
  `ui-program-intake`, and `codex.platform-ui-baseline/v1@2.0.0` baseline.
- Preserve the user's accepted product path, scope, roles, journeys, mobile
  exclusion, and visual direction.
- Correct the sign-in/recovery screen and state identities.
- Capture a hash-bound source inventory from the exact accepted successor pilot
  bytes.
- Create an exact-cover standard clause inventory: every reusable observation
  maps to exactly one stable clause and baseline JSON pointer; every
  intentionally non-reusable observation has one typed source-backed
  disposition.
- Cover foundations, complete typography, controls and states, component
  anatomy, spacing, sizing, radii, borders, elevation, opacity, icons, focus,
  motion, shell/navigation, layouts, responsive range/anchors/transforms,
  overflow, accessibility and copy policies where the source fixes them.
- Do not reduce the pilot to one token per category, one component, or one
  interaction.
- Prove source freshness, exact accepted visual authority, deterministic
  repeatability, no circular target-authored standard, and `baseline_ready`.
- Render one compact baseline/standard review board and obtain exact typed
  owner acceptance of the successor baseline revision, clause inventory and
  exact visual-authority source before accepting G0. This checkpoint is
  mandatory even when the accepted pilot bytes remain unchanged. If the exact
  visual-authority bytes changed only for semantic metadata, include that
  candidate and its equivalence proof in the same decision packet.

## G1 successor

- Rebuild the exact-cover atlas under the active program revision.
- Preserve every authoritative route, route flow, persistent shell,
  route-backed transient, overlay, system surface, internal/non-visual surface,
  and justified historical exclusion exactly once.
- Correct `UI-AUTH-001` and `UI-AUTH-004` identity/state/action bindings.
- Preserve the existing 175-screen scope unless live authoritative sources
  prove a revision is necessary; report any exact inventory delta.
- Rebuild hash-pinned screen and journey indexes and pass `atlas_gate`.
- Do not add a routine owner gate.

## G2 successor

- Rebuild journeys, criticality, families, representatives, coverage profiles,
  workload-bounded waves, family and wave indexes under the active program.
- Bind every family to the exact active `{baseline_id,path,sha256}` plus
  `standard_revision_id`, `clause_inventory_sha256`, deterministic
  applicability policy, and exact exception policy.
- Bind `UI-AUTH-001` and `UI-AUTH-004` as the two exact representatives of the
  single auth family described above; keep workload budgets truthful and stop
  if this topology cannot validate.
- Preserve the existing intended 26-family/four-wave topology only where the
  corrected semantics and active contract still justify it. Any deterministic
  split or merge must be recorded through exact-cover indexes and successor
  stage rows, not hidden in prose.
- Rebuild every future current-authority G4/G5/G6 prompt and pending ledger row
  against runtime `2.0.0`; supersede the old pending rows without claiming or
  executing them.
- Pass `structure_gate` and adjacent G3 preflight.
- Do not add a routine owner gate.

## G3 successor

- Realize the fixed active baseline without changing its identities.
- Create and validate the active representative shell screen contract under
  screen contract `2.0.0`.
- Capture the unchanged accepted pilot as `visual_authority_reference` and the
  target shell as `screen_implementation` with asymmetric proof scope.
- Produce the complete standard board and clause-exact inheritance report,
  including foundations, typography, components/states, shell/layout,
  responsive behavior, focus/motion, and material exceptions.
- Replace the old hand-picked inheritance subset with exact clause coverage.
- Produce render provenance, geometry/computed-style evidence, interaction,
  console, network, responsive, keyboard/focus, 200% zoom, RU/EN, reduced
  motion, accessibility-smoke, overflow, deterministic and freshness proof.
- Create a hash-bound review-board manifest and exact rendered
  `data-review-entry` closure.
- Reach `review_ready`, set the successor row to `needs_input`, ledger to
  `awaiting_input`, keep `next_stage_allowed: false`, and present the finished
  G3 board to the owner.
- Do not self-accept G3. Continue only after an unambiguous natural-language
  owner acceptance is assembled into the canonical typed response/decision and
  the strict G3 gate plus transition are rerun as `ready`.

## G4 auth-family successor

- After accepted G3 only, claim the unique successor auth-family G4 row.
- Create active screen contracts with `standard_binding`,
  `standard_exceptions`, `component_state_ids`, `standard_slot_id`, exhaustive
  visible element inventory, exact route/state/action semantics, and corrected
  representatives.
- Derive a canonical standard-applicability manifest for every representative
  screen contract.
- Capture every required representative state and responsive anchor with
  target-side computed standard observations.
- Apply full target checks only to `screen_implementation`; keep the pilot
  `visual_authority_reference` immutable and free from target-only obligations.
- Assemble active render provenance, visual-QA, screen acceptance, exact-cover
  family acceptance, and review-board artifacts. Never hand-edit receipts.
- Require `standard_conformance` per target anchor, `standard_coverage` per
  screen acceptance, and `standard_compliance` in the family aggregate.
- Render exact family-aggregate and nested evidence closure in the owner board.
- Re-run all browser evidence affected by the four carried owner corrections.
- Pass `family_review_ready` and adjacent-stage preflight, but do not accept the
  family or claim another G4/G5/G6 row.
- Assemble a `review_ready` transition with `next_stage_allowed: false`.
- Set the same successor G4 row to `needs_input`, ledger to `awaiting_input`,
  preserve its executor claim, and bind one finished-result decision packet.

## Historical lifecycle and ledger migration

- Preserve every historical artifact and receipt byte-for-byte.
- Never rewrite an old `generated_by.version` to make it pass an active schema.
- Move invalidated accepted rows out of current authority through the typed
  change-impact/supersession lifecycle. Preserve `historical_outcome: accepted`
  where applicable.
- Keep blocked G4 r2 historical, blocked, and current-authority false.
- Keep unaccepted G4 r3 historical and unaccepted; do not convert its current
  review into owner acceptance.
- Preserve historical executor claims and evidence references.
- Create exactly one current-authority successor chain and prevent competing
  claims.
- Supersede old pending G4/G5/G6 rows and create new pending current-authority
  successors under active prompts. Leave them unclaimed except the authorized
  auth-family successor.
- Ensure `validate_stage_ledger.py` no longer attempts to validate historical
  receipts as if they were newly assembled active `2.0.0` receipts.
- Do not create a second state file.

## Documentation and normative requirement continuity

Update only the current canonical documents needed to prevent renewed drift:

- `.codex/AGENTS.md`: clarify that visual-language authority permits novel
  target composition but requires every applicable reusable pilot clause; it
  is not permission for aesthetic approximation.
- `custometry-ui-blueprint-ru.md`: reconcile stale G0/G2 frontier statements,
  the previously absent-baseline language, pilot-standard clause inheritance,
  deterministic applicability, typed exceptions, and the corrected
  sign-in/recovery boundary.
- `custometry-technical-blueprint-ru.md`: add or extend normative UI-program
  requirements for exact source inventory, clause lineage, deterministic
  applicability, narrow exceptions, target-semantic ownership, and
  non-circular computed proof without inventing unrelated product scope.
- `custometry-technical-blueprint-human-ru.md`: keep the required explanatory
  mirror synchronized.
- `docs/generated/requirement-index.json` and affected generated route/surface
  contracts: regenerate only through their canonical repository mechanics when
  the normative source change requires it.

Do not revive Penpot/Figma or historical target authority. Do not make the
pilot own target screen content, fixture values, permissions, data semantics,
or exact novel-screen composition.

# Context acquisition and skill routing

Before task actions, read completely:

```text
AGENTS.md
.codex/AGENTS.md
/Users/daniildegtyarev/.codex/skills/ui-design-program/SKILL.md
/Users/daniildegtyarev/.codex/skills/staged-plan-runner/SKILL.md
/Users/daniildegtyarev/.codex/skills/prompt-manager/SKILL.md
```

Because this task migrates program contracts and prompts, also read the complete
maintenance references selected by those skills:

```text
/Users/daniildegtyarev/.codex/skills/ui-design-program/references/ui-design-program-contract-v1.md
/Users/daniildegtyarev/.codex/skills/ui-design-program/references/stage-transition-contract-v1.md
/Users/daniildegtyarev/.codex/skills/ui-design-program/references/stage-profiles-v1.md
/Users/daniildegtyarev/.codex/skills/ui-design-program/references/intake-baseline-contract-v1.md
/Users/daniildegtyarev/.codex/skills/ui-design-program/references/visual-provenance-contract-v1.md
/Users/daniildegtyarev/.codex/skills/ui-design-program/references/responsive-policy-v1.md
/Users/daniildegtyarev/.codex/skills/prompt-manager/references/prompt-pack-artifacts-v1.md
```

During the bounded shared-skill repair, also read the complete `skill-creator`
instructions and only the maintenance sources directly required to implement
and test the canonical migration surface. Do not carry that maintenance context
into ordinary G0-G4 stage execution.

Use `prompt-manager` only for triad/prompt compatibility migration and
`staged-plan-runner` for claims and ledger lifecycle. At G3/G4 browser work,
read `browser-qa-evidence` and its browser mechanics contract completely, then
select exactly `playwright-cli` as the canonical terminal browser mechanic.
Do not open a competing Browser or Chrome mechanic for gate evidence.

Load a `better-*` craft skill only if an observed defect or required decision
materially crosses that domain. Do not use `ui-ux-pro-max`, Product Design,
Figma, Penpot, mobile design, or a new design-direction route; the visual
direction is already accepted.

Before any claim, use the active skill's current ledger-bound context command:

```text
python3 /Users/daniildegtyarev/.codex/skills/ui-design-program/scripts/ui_program_context.py \
  --ledger .codex/delivery/ui-design-programs/custometry-v2/stage-ledger.md \
  --project-root /Users/daniildegtyarev/.codex/worktrees/e0a0/Custometry
```

The current historical triad is expected to block this command before control-
plane migration. Migrate deterministic control fields and create the unique
successor frontier before selection; do not claim a known-invalid row. After
the ledger validates, generate bounded preclaim context, atomically claim the
unique row, regenerate context, and consume only the post-claim manifest for
stage execution. Respect the active maximum of 16 resolved files and about
600k estimated text tokens per stage.

# Required execution sequence

1. Revalidate the exact root, dirty worktree, triad, hashes, skill release,
   accepted decisions, historical/current authority, claims and absence of a
   competing writer.
2. Re-run the skill release, capture-chain and standard regression checks.
3. Implement, test and release the narrowly scoped canonical historical-
   control-plane migration repair. Do not touch Custometry lifecycle state
   until the repaired release passes.
4. Reproduce the canonical real-pilot capture in a fresh temporary directory.
5. Use the released two-phase migration surface to bind the exact historical
   ledger, assemble typed compatibility/change-impact evidence and validate the
   unique successor topology before atomically applying it.
6. Build and prove the revisioned metadata-only pilot candidate if semantic
   identities remain insufficient. Pause for the exact owner decision before
   accepting the successor baseline and visual authority.
7. Migrate program-owned active schemas/templates/registry and the triad under
   `prompt-manager`; preserve historical artifacts and prepare unique pending
   successor rows.
8. Validate the ledger and use `staged-plan-runner` to execute the G0 successor
   to its mandatory baseline owner checkpoint. Accept it only after the exact
   typed response and strict rerun.
9. Execute G1 and G2 successors sequentially with validated transitions and no
   routine owner gate.
10. Execute G3 to `review_ready`; pause and obtain exact natural-language owner
   acceptance. Assemble the canonical response, rerun strict G3 and accept it.
11. Claim only the successor auth-family G4 row and build the finished result.
12. Run all active runtime-profile browser, acceptance, aggregate, review-board,
    freshness, inheritance, write-scope, foreign-change, determinism, ledger
    and adjacent-stage checks.
13. Assemble and validate the G4 `review_ready` transition.
14. Stop at the requested terminal state and ask exactly one owner decision:
    accept the finished auth-family result or request bounded corrections.

Every stage boundary must be recorded durably before the next row is selected.
Do not batch state mutations across an owner checkpoint.

# Minimum validation boundary

Run every exact command emitted by the fresh active runtime profiles. At a
minimum, include the current forms of:

```text
python3 /Users/daniildegtyarev/.codex/skills/ui-design-program/scripts/validate_skill_release.py
python3 /Users/daniildegtyarev/.codex/skills/ui-design-program/scripts/validate_ui_standard_capture_chain.py --self-test
python3 /Users/daniildegtyarev/.codex/skills/ui-design-program/scripts/validate_ui_standard_regression.py --self-test
python3 /Users/daniildegtyarev/.codex/skills/ui-design-program/scripts/ui_design_tool.py preflight
python3 /Users/daniildegtyarev/.codex/skills/ui-design-program/scripts/validate_stage_ledger.py --project-root <root> <ledger>
python3 /Users/daniildegtyarev/.codex/skills/ui-design-program/scripts/assemble_stage_transition.py --request <request> --project-root <root> --ledger <ledger> --output <receipt>
python3 /Users/daniildegtyarev/.codex/skills/ui-design-program/scripts/validate_stage_transition.py --project-root <root> --ledger <ledger> <receipt>
source scripts/activate-toolchain.sh
uv run python -m tools.check --scope local
```

Also run, as emitted for the active stages:

- `intake_ready`, `baseline_ready`, `atlas_gate`, `structure_gate`, and
  `program_ready`;
- `screen_contract_ready` for every active G3/G4 representative contract;
- canonical standard capture and standard applicability assembly;
- Playwright render provenance, geometry/computed styles, interaction,
  console, network, responsive-Web, RU/EN, 200% zoom, reduced motion,
  keyboard/focus, accessibility-smoke, overflow and deterministic evidence;
- `visual_acceptance`, screen acceptance and family acceptance assembly;
- `family_review_ready`;
- review-board manifest/HTML nested closure and exact rendered entries;
- source freshness, fixed inheritance, write scope, foreign-change exclusion
  and adjacent-stage preflight.

A global raster score is supporting evidence only. Blocking standard proof
comes from source-derived clauses, deterministic applicability, computed target
values, behavior, geometry and readable content. A green repository-local gate
does not substitute for browser evidence.

# Allowed writes

Ordinary writes are authorized only within the following current-program zones
when needed by this migration:

```text
.codex/delivery/ui-design-programs/custometry-v2/**
.codex/agents/generated/custometry-ui-design-g0-v2/**
.codex/delivery/evidence/custometry-ui-design-program-v2/**
.codex/AGENTS.md
custometry-ui-blueprint-ru.md
custometry-technical-blueprint-ru.md
custometry-technical-blueprint-human-ru.md
docs/generated/requirement-index.json
packages/contracts/routes/ui-surface-contracts.json
```

The last six paths may be changed only for the exact normative/derived
continuity described above. Preserve unrelated hunks. If another current
canonical derived file must change, prove the direct generator/source link and
record it as `outside_expected_paths` before writing; otherwise stop.

The installed `/Users/daniildegtyarev/.codex/skills/ui-design-program/**` tree
is writable only for the exact historical-control-plane migration repair and
its release tests. Treat it as read-only after that bounded unit passes.

# Safety and non-goals

- Inspect the dirty worktree before each material write and preserve every
  foreign change. Do not clean, stash, reset, checkout, reconstruct, or revert
  it.
- Do not commit, push, open a pull request, publish, deploy, create a branch or
  worktree, use secrets, exercise paid/external actions, or mutate production.
- Do not modify production application code.
- Do not alter historical accepted artifacts or receipts.
- Do not self-accept a visual-authority candidate, G3, or G4.
- Do not resume blocked G4 r2 or accept historical G4 r3.
- Do not claim another G4 family, any G5 wave, or G6.
- Do not add mobile-specific scope.
- Do not copy target content, fixture values, analytical thresholds, or product
  semantics from the pilot.
- Do not invent a second state authority, acceptance shortcut, global latest
  receipt, magic owner command, or hand-edited passing evidence.

# Stop conditions

Stop before the affected mutation and report a precise blocker if any of these
is true:

- the exact live root or triad is missing or points elsewhere;
- a dispatch hash changed and live sources do not explain the change;
- the skill release or active registry changed materially and the new release
  does not pass its own release/capture/regression gates;
- the live ledger frontier or current-authority row changed materially;
- a competing claim or writer exists;
- accepted decisions, repair evidence, transition receipts, or source hashes
  are unreadable or mismatched;
- the dirty worktree contains an inseparable foreign hunk in a required file;
- the real pilot capture cannot be made deterministic or exact;
- the shared-skill repair cannot provide a canonical non-circular first
  migration operation without weakening active validation;
- the metadata candidate changes any visible, behavioral, responsive, focus,
  network, console, asset or font result;
- stable source identities remain generic, conflicting, duplicate, stale or
  unbounded after the compatibility candidate;
- the source inventory cannot exact-cover every reusable observation and typed
  disposition without weakening the active validator;
- the auth sign-in/recovery meaning conflicts with a higher current product
  source;
- a required owner checkpoint is pending or rejected;
- the ledger-bound context is unbounded, misdirected, or not uniquely
  selectable;
- active gate proof requires another shared-skill change outside the one
  authorized repair or any production-code change;
- the terminal G4 family cannot pass under the active contract.

Use `needs_input` only for the exact recoverable owner checkpoints. Use hard
`blocked` only for authority, safety, unavailable evidence, permission,
inseparable foreign change, or an unrecoverable contract/tooling condition.

# Acceptance criteria

The task is complete only when all are true:

1. The active skill release and real capture chain are independently verified.
2. The legacy pilot semantic-identity gap is closed without changing historical
   accepted bytes or weakening canonical proof.
3. The shared skill exposes and tests one canonical non-circular historical-
   ledger migration route, and the full skill release passes.
4. Any successor visual-authority bytes are proven metadata-only, and the exact
   successor G0 baseline/visual authority is accepted through a typed owner
   decision regardless of whether the source bytes changed.
5. Program, baseline, screen, runtime and assembler profiles match the active
   registry.
6. The active baseline has an exact source inventory and exact-cover standard
   clauses; no sparse symbolic baseline remains active.
7. `UI-AUTH-001 /auth/sign-in` and `UI-AUTH-004 /auth/recovery` have distinct,
   source-backed route/surface/state/action contracts.
8. One workload-valid auth family exact-covers both screens as representatives;
   no second auth-family G4 row is created or claimed.
9. Every family has exact baseline/standard/applicability binding.
10. Historical rows/receipts remain byte-identical and read-only; active ledger
   validation no longer treats them as new active receipts.
11. Every future current-authority G4/G5/G6 row uses active prompts and remains
   unclaimed except the authorized auth-family successor.
12. G3 active standard board, inheritance proof, browser evidence and nested
    review closure pass and the owner has accepted the exact finished result.
13. The auth-family successor carries forward all four owner corrections and
    passes every active G4 `family_review_ready` requirement.
14. The final ledger and G4 transition match the requested terminal YAML with
    `next_stage_allowed: false` and owner acceptance pending.
15. The repository local gate passes or every unrelated/pre-existing failure is
    truthfully classified without overstating proof.
16. The final report names actual changed paths, validation results, observed
    proof boundary, preserved foreign changes and residual risk.

# Final owner-facing report

Write the final owner-facing report in Russian. Repository-authored artifacts
remain English except the existing Russian blueprint files.

Lead with the finished auth-family result being reviewed. Explain, concisely:

- that compatibility with the current pilot-standard contract is complete;
- which sign-in and recovery screens/states are represented;
- how the accepted pilot now governs typography, controls, radii, palette,
  density, focus, motion and responsive behavior without dictating target
  product meaning or composition;
- that sign-in is compact, recovery is a distinct route flow, review controls
  are interactive, and marketing copy is absent;
- material allowed differences and proof limits;
- the validation boundary, changed paths, residual risks and preserved foreign
  changes;
- that historical r2/r3 evidence was preserved and no adjacent row was claimed.

Show no more than five useful visual links. Ask exactly one decision: accept the
finished auth-family result or request bounded corrections.
